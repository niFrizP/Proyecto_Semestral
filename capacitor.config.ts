import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'proyecto_semestral',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
